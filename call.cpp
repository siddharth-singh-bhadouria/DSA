#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t;
    cin >> t;
    int arr[4];
    vector<int> moments;
    while (t--)
    {
        for (int i = 0; i < 4; i++)
        {
            cin >> arr[i];
        }
        for (int i = 0;; i++)
        {
            int a;
            cin >> a;
            data.pushback(a);
        }
    }
    return 0;
}