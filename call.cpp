#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t;
    cin >> t;
    vector<int> data, moments;
    while (t--)
    {
        for (int i = 0; i < n; i++)
        {
            data.pushback()
        }
    }
    return 0;
}